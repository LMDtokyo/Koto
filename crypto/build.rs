fn main() {
    // uniffi scaffolding is generated via proc-macros in src/lib.rs
    // (#[uniffi::export], #[derive(uniffi::Object/Record/Error)]).
    // No UDL file is used, so nothing to do here.
}
