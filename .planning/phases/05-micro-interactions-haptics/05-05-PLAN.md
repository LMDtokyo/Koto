---
phase: 05-micro-interactions-haptics
plan: 05
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified: []
autonomous: false
requirements: [MI-01, MI-02, MI-03, MI-04, MI-05]

must_haves:
  truths:
    - "All 5 MI requirements verified visually on a physical device or emulator"
    - "No raw spring() literals remain in any file touched by Phase 5 plans"
    - "Build succeeds: ./gradlew assembleDebug exits 0"
  artifacts:
    - path: "android/app/src/main/java/run/koto/ui/components/atoms/MorphingSendButton.kt"
      provides: "verified morph"
      contains: "SendButtonState"
    - path: "android/app/src/main/java/run/koto/ui/components/atoms/AvatarProfileCard.kt"
      provides: "verified popup"
      contains: "springEmphasized"
    - path: "android/app/src/main/java/run/koto/ui/components/atoms/ReactionParticleOverlay.kt"
      provides: "verified heart react"
      contains: "HeartReaction"
    - path: "android/app/src/main/java/run/koto/ui/theme/Haptics.kt"
      provides: "verified haptic utility"
      contains: "rememberKotoHaptics"
  key_links:
    - from: "Phase 5 plans"
      to: "KotoTheme.motion spring specs"
      via: "all animation specs reference named constants"
      pattern: "KotoTheme\\.motion\\.spring"
---

<objective>
Final verification checkpoint for Phase 5. Run the full build, perform an automated spring-literal audit, and confirm all MI requirements pass visual inspection on device.

Purpose: All 5 MI requirements (MI-01 through MI-05) need a single consolidated verification pass before Phase 5 is declared complete.
Output: Build passes, spring audit passes, human confirms visual interactions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Full build + spring literal audit</name>
  <files></files>
  <action>
Run the following checks in sequence:

**1. Full debug build:**
```bash
cd android && ./gradlew assembleDebug
```
Must exit 0. If any compilation error, fix it before continuing.

**2. Spring literal audit (MI-05) — find any raw spring() calls with numeric literals in Phase 5 files:**
```bash
grep -rn "spring(dampingRatio\|spring(stiffness\|spring(0\." \
    android/app/src/main/java/run/koto/ui/components/atoms/MorphingSendButton.kt \
    android/app/src/main/java/run/koto/ui/components/atoms/AvatarProfileCard.kt \
    android/app/src/main/java/run/koto/ui/components/atoms/ReactionParticleOverlay.kt \
    android/app/src/main/java/run/koto/ui/theme/Haptics.kt \
    android/app/src/main/java/run/koto/ui/theme/Motion.kt \
    2>/dev/null
```
Expected: zero matches. If any match is found, replace the raw literal with the appropriate `KotoTheme.motion.spring*` constant.

**3. Verify all 5 haptic wiring points exist in ChatScreen:**
```bash
grep -c "haptics\." android/app/src/main/java/run/koto/ui/screens/chat/ChatScreen.kt
```
Expected: 5 or more matches (onSend, onLongPress, onSwipeThreshold, onDoubleTap, onReactionSettle).

**4. Verify all new atoms exist:**
```bash
ls android/app/src/main/java/run/koto/ui/components/atoms/
```
Expected: MorphingSendButton.kt, AvatarProfileCard.kt, ReactionParticleOverlay.kt, TypingWaveIndicator.kt present.

**5. Verify KotoMotion has 5 spring specs:**
```bash
grep -c "SpringSpec" android/app/src/main/java/run/koto/ui/theme/Motion.kt
```
Expected: 5 (springSnappy, springBouncy, springGentle, springMicro, springEmphasized).

If any check fails, fix the issue before calling the checkpoint.
  </action>
  <verify>
    <automated>cd C:/Users/vnaio/Desktop/koto/android && ./gradlew assembleDebug 2>&1 | tail -10</automated>
  </verify>
  <done>assembleDebug exits 0. Spring audit returns 0 raw literals in Phase 5 files. 5+ haptic call sites in ChatScreen. 3 new atom files present. 5 SpringSpec entries in Motion.kt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All Phase 5 micro-interactions are implemented:
- MI-01: MorphingSendButton (mic → arrow → spinner → checkmark → mic)
- MI-02: Double-tap heart reaction with spring bounce + float-fade
- MI-03: Haptics at send, long-press, swipe threshold, double-tap, reaction settle
- MI-04: Avatar long-press profile card with blur backdrop + spring entry
- MI-05: All animations use named KotoMotion spring specs
  </what-built>
  <how-to-verify>
Install the debug APK on a physical device (preferred) or API 31+ emulator:
```
cd android && ./gradlew installDebug
```

Test each requirement:

**MI-01 — Send button morph:**
1. Open any conversation
2. Observe: send button shows a mic icon when text field is empty
3. Type any text → button should morph to send arrow (spring animation, not instant)
4. Tap send → button should briefly show CircularProgressIndicator, then a checkmark for ~800ms, then return to mic
5. Type text again → morphs to arrow

**MI-02 — Double-tap react:**
1. Open any conversation with messages
2. Double-tap a message bubble
3. Observe: heart emoji springs up from 0%, overshoots to ~120%, settles at 100%, then floats upward 24dp while fading out over ~600ms
4. On a device with haptics: feel a CLOCK_TICK on the tap and a CONFIRM on heart settle

**MI-03 — Haptics (physical device only):**
1. Tap Send → distinct CONFIRM haptic
2. Long-press a message → distinct LONG_PRESS haptic
3. Swipe right on a message bubble past 48dp threshold → CONTEXT_CLICK snap haptic
4. Double-tap a message → CLOCK_TICK then CONFIRM 800ms later
5. Swipe conversation list item past threshold → CONTEXT_CLICK haptic

**MI-04 — Avatar profile card:**
1. Go to conversation list
2. Long-press any conversation avatar (hold 400ms)
3. Observe: profile card springs in from center (scale 0 → 100%), blur/scrim backdrop covers screen
4. Card shows: 80dp avatar, name, online status, "Написать" button, Call + Profile outline buttons
5. Tap outside card → spring-exit (scale 100% → 0%)

**MI-05 — Spring animations everywhere:**
Review all animations visually — everything should feel springy (slight overshoot, natural settle). Nothing should have a linear or ease-in-out curve feel:
1. Send button state changes — springy
2. Heart reaction enter — bouncy overshoot
3. Profile card entry/exit — spring settle
4. Context menu entry — spring scale
5. FAB appear/disappear — spring
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 requirements pass, or describe specific failures to fix</resume-signal>
</task>

</tasks>

<verification>
1. `./gradlew assembleDebug` exits 0
2. Zero raw `spring(dampingRatio` or `spring(stiffness` literals in Phase 5 files
3. 5+ haptic call sites in ChatScreen.kt
4. All 5 MI requirements verified on device by human
</verification>

<success_criteria>
- Build passes
- Spring audit clean (MI-05)
- Human confirms all 5 MI requirements work correctly on device
- Phase 5 complete
</success_criteria>

<output>
After completion, create `.planning/phases/05-micro-interactions-haptics/05-05-SUMMARY.md`
</output>
