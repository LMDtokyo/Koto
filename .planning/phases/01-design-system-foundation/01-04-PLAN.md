---
phase: 01-design-system-foundation
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - android/app/src/main/java/run/koto/ui/theme/Spacing.kt
  - android/app/src/main/java/run/koto/ui/theme/Shapes.kt
  - android/app/src/main/java/run/koto/ui/theme/Elevation.kt
  - android/app/src/main/java/run/koto/ui/theme/Motion.kt
autonomous: true
requirements: [DS-04, DS-05]

must_haves:
  truths:
    - "KotoSpacing data class exists with xxs=2dp through xxxl=48dp plus messenger semantic tokens"
    - "KotoShapes data class exists with sm=8dp, md=12dp, lg=16dp, xl=20dp, bubble=18dp, asymmetric bubbleOut/bubbleIn"
    - "KotoMotion data class exists with springSnappy, springBouncy, springGentle, tweenFast, tweenMedium, tweenSlow"
    - "KotoElevation data class exists with none=0dp through dialog=8dp"
  artifacts:
    - path: "android/app/src/main/java/run/koto/ui/theme/Spacing.kt"
      provides: "@Immutable data class KotoSpacing with all spacing tokens"
      contains: "val xxxl : Dp = 48.dp"
    - path: "android/app/src/main/java/run/koto/ui/theme/Shapes.kt"
      provides: "@Immutable data class KotoShapes with all shape tokens"
      contains: "val bubble"
    - path: "android/app/src/main/java/run/koto/ui/theme/Elevation.kt"
      provides: "@Immutable data class KotoElevation"
      contains: "val dialog"
    - path: "android/app/src/main/java/run/koto/ui/theme/Motion.kt"
      provides: "@Immutable data class KotoMotion + DefaultKotoMotion instance"
      contains: "val springSnappy"
  key_links:
    - from: "android/app/src/main/java/run/koto/ui/theme/Theme.kt"
      to: "android/app/src/main/java/run/koto/ui/theme/Spacing.kt"
      via: "LocalKotoSpacing provides KotoSpacing() in CompositionLocalProvider"
      pattern: "KotoSpacing\\(\\)"
    - from: "android/app/src/main/java/run/koto/ui/theme/Theme.kt"
      to: "android/app/src/main/java/run/koto/ui/theme/Motion.kt"
      via: "LocalKotoMotion provides DefaultKotoMotion"
      pattern: "DefaultKotoMotion"
---

<objective>
Create four token files: Spacing, Shapes, Elevation, and Motion — the remaining stateless
token categories that do not vary between light and dark themes.

Purpose: DS-04 (spacing scale) and DS-05 (shape system) are required by all downstream
composables. Motion tokens are needed to prove KotoMotionSpec pattern exists (Phase 1 success
criterion 5). Elevation tokens are structural — needed by Plan 05 to populate CompositionLocal.

Output:
- Spacing.kt: KotoSpacing with xxs=2dp through xxxl=48dp + messenger semantic tokens
- Shapes.kt: KotoShapes with standard sizes + asymmetric bubble shapes + messenger-specific shapes
- Elevation.kt: KotoElevation with 7 levels
- Motion.kt: KotoMotion with 3 spring specs + 3 tween specs + DefaultKotoMotion instance
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-system-foundation/01-RESEARCH.md
@.planning/phases/01-design-system-foundation/01-01-SUMMARY.md

<interfaces>
<!-- What Plan 05 (Theme.kt full provider) will consume from this plan. -->

From Spacing.kt:
```kotlin
@Immutable
data class KotoSpacing(
    val xxs : Dp = 2.dp,  val xs  : Dp = 4.dp,  val sm : Dp = 8.dp,
    val md  : Dp = 12.dp, val lg  : Dp = 16.dp, val xl : Dp = 20.dp,
    val xxl : Dp = 24.dp, val xxxl: Dp = 48.dp,
    val bubblePaddingH : Dp = 14.dp, val bubblePaddingV  : Dp = 10.dp,
    val listItemPadding: Dp = 16.dp, val inputBarPadding : Dp = 12.dp,
)
```

From Shapes.kt:
```kotlin
@Immutable
data class KotoShapes(
    val bubbleOut  : Shape, val bubbleIn   : Shape,
    val sm         : Shape, val md         : Shape,
    val lg         : Shape, val xl         : Shape,
    val bubble     : Shape, val bottomSheet: Shape,
    val inputField : Shape, val pill       : Shape, val card: Shape,
)
```

From Elevation.kt:
```kotlin
@Immutable
data class KotoElevation(
    val none: Dp, val low: Dp, val card: Dp,
    val appBar: Dp, val fab: Dp, val dialog: Dp, val pressed: Dp,
)
```

From Motion.kt:
```kotlin
@Immutable
data class KotoMotion(
    val springSnappy: SpringSpec<Float>,
    val springBouncy: SpringSpec<Float>,
    val springGentle: SpringSpec<Float>,
    val tweenFast   : TweenSpec<Float>,
    val tweenMedium : TweenSpec<Float>,
    val tweenSlow   : TweenSpec<Float>,
)
val DefaultKotoMotion: KotoMotion
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Spacing.kt and Shapes.kt</name>

  <read_first>
    - .planning/research/FEATURES.md lines 458-498 (spacing scale table and corner radius scale table — exact dp values)
    - .planning/phases/01-design-system-foundation/01-RESEARCH.md (DS-04 xxxl=48dp conflict note, DS-05 shapes code)
  </read_first>

  <files>
    android/app/src/main/java/run/koto/ui/theme/Spacing.kt,
    android/app/src/main/java/run/koto/ui/theme/Shapes.kt
  </files>

  <behavior>
    KotoSpacing().xxs == 2.dp
    KotoSpacing().xxxl == 48.dp  (NOT 32.dp — REQUIREMENTS.md is authoritative over ARCHITECTURE.md)
    KotoSpacing().lg == 16.dp
    KotoSpacing().bubblePaddingH == 14.dp
    KotoShapes has fields: bubbleOut, bubbleIn, sm, md, lg, xl, bubble, bottomSheet, inputField, pill, card
    KotoShapes().sm uses RoundedCornerShape(8.dp)
    KotoShapes().bubble uses RoundedCornerShape(18.dp) — DS-05 bubble outer corners
    KotoShapes().bubbleOut asymmetric: topStart=18dp, topEnd=18dp, bottomEnd=4dp, bottomStart=18dp
    KotoShapes().bubbleIn asymmetric:  topStart=18dp, topEnd=18dp, bottomEnd=18dp, bottomStart=4dp
  </behavior>

  <action>
    STEP 1 — Create android/app/src/main/java/run/koto/ui/theme/Spacing.kt:

    ```kotlin
    package run.koto.ui.theme

    import androidx.compose.runtime.Immutable
    import androidx.compose.ui.unit.Dp
    import androidx.compose.ui.unit.dp

    /**
     * Spacing design tokens for KotoTheme.
     * Access via KotoTheme.spacing.* — never use raw dp literals in layout code.
     *
     * DS-04: xxs=2dp through xxxl=48dp. Messenger-semantic tokens for common use cases.
     * Note: xxxl=48dp per REQUIREMENTS.md DS-04 (overrides ARCHITECTURE.md which had 32dp).
     */
    @Immutable
    data class KotoSpacing(
        // ── Base scale ────────────────────────────────────────────────────────────
        val xxs  : Dp =  2.dp,   // between check marks, dot indicators
        val xs   : Dp =  4.dp,   // inline icon-to-text gap
        val sm   : Dp =  8.dp,   // between grouped bubbles, chip padding
        val md   : Dp = 12.dp,   // avatar-to-text gap, section internal padding
        val lg   : Dp = 16.dp,   // screen horizontal padding, between sections
        val xl   : Dp = 20.dp,   // between major UI groups (FEATURES.md xl=24dp; using 20dp per arch)
        val xxl  : Dp = 24.dp,   // between major UI groups
        val xxxl : Dp = 48.dp,   // empty state vertical centering (DS-04: 48dp)
        // ── Messenger-semantic convenience tokens ─────────────────────────────────
        val bubblePaddingH  : Dp = 14.dp,   // bubble horizontal padding
        val bubblePaddingV  : Dp = 10.dp,   // bubble vertical padding
        val listItemPadding : Dp = 16.dp,   // conversation row horizontal padding
        val inputBarPadding : Dp = 12.dp,   // compose bar horizontal padding
    )
    ```

    STEP 2 — Create android/app/src/main/java/run/koto/ui/theme/Shapes.kt:

    ```kotlin
    package run.koto.ui.theme

    import androidx.compose.foundation.shape.CircleShape
    import androidx.compose.foundation.shape.RoundedCornerShape
    import androidx.compose.runtime.Immutable
    import androidx.compose.ui.graphics.Shape
    import androidx.compose.ui.unit.dp

    /**
     * Shape design tokens for KotoTheme.
     * Access via KotoTheme.shapes.* — never hardcode corner radii in composables.
     *
     * DS-05: sm=8dp, md=12dp, lg=16dp, xl=20dp, bubble outer corners=18dp.
     * Chat bubble shapes are asymmetric: the tail-adjacent corner is 4dp (inner corner),
     * outer corners are 18dp. bubbleOut = outgoing (tail bottom-right), bubbleIn = incoming.
     */
    @Immutable
    data class KotoShapes(
        // ── Chat bubbles (asymmetric — tail corner = 4dp per FEATURES.md) ─────────
        // RoundedCornerShape(topStart, topEnd, bottomEnd, bottomStart)
        val bubbleOut  : Shape = RoundedCornerShape(18.dp, 18.dp,  4.dp, 18.dp),  // tail bottom-right
        val bubbleIn   : Shape = RoundedCornerShape(18.dp, 18.dp, 18.dp,  4.dp),  // tail bottom-left
        // ── Standard shapes (DS-05) ──────────────────────────────────────────────
        val sm         : Shape = RoundedCornerShape( 8.dp),   // input fields, chips
        val md         : Shape = RoundedCornerShape(12.dp),   // media thumbnails, context menus
        val lg         : Shape = RoundedCornerShape(16.dp),   // cards, nav elements
        val xl         : Shape = RoundedCornerShape(20.dp),   // modals, large cards
        val bubble     : Shape = RoundedCornerShape(18.dp),   // bubble outer corners (DS-05)
        // ── Messenger-specific ────────────────────────────────────────────────────
        val bottomSheet: Shape = RoundedCornerShape(topStart = 24.dp, topEnd = 24.dp),
        val inputField : Shape = RoundedCornerShape(24.dp),   // pill compose bar
        val pill       : Shape = CircleShape,                 // avatars, badges, FAB
        val card       : Shape = RoundedCornerShape(16.dp),   // floating cards
    )
    ```

    STEP 3 — Verify compile:
    cd /c/Users/vnaio/Desktop/koto/android && ./gradlew :app:assembleDebug 2>&1 | tail -10
  </action>

  <verify>
    <automated>cd /c/Users/vnaio/Desktop/koto/android && ./gradlew :app:assembleDebug 2>&1 | grep -E "BUILD (SUCCESSFUL|FAILED)"</automated>
  </verify>

  <acceptance_criteria>
    - android/app/src/main/java/run/koto/ui/theme/Spacing.kt exists
    - Spacing.kt contains "val xxxl : Dp = 48.dp" (48, not 32)
    - Spacing.kt contains "val xxs  : Dp =  2.dp"
    - Spacing.kt contains "val lg   : Dp = 16.dp"
    - Spacing.kt contains "@Immutable" and "data class KotoSpacing"
    - android/app/src/main/java/run/koto/ui/theme/Shapes.kt exists
    - Shapes.kt contains "@Immutable" and "data class KotoShapes"
    - Shapes.kt contains "val bubble" and "RoundedCornerShape(18.dp)"
    - Shapes.kt contains "val bubbleOut" and "val bubbleIn" with asymmetric shapes (4.dp tail corner)
    - Shapes.kt contains "val sm" with RoundedCornerShape(8.dp)
    - ./gradlew :app:assembleDebug → BUILD SUCCESSFUL
    - KotoThemeTest: "spacing xxs is 2dp" passes
    - KotoThemeTest: "spacing xxxl is 48dp" passes
    - KotoThemeTest: "spacing lg is 16dp" passes
    - KotoThemeTest: "shapes sm is 8dp rounded" passes
    - KotoThemeTest: "shapes bubble outer corner is 18dp" passes
  </acceptance_criteria>

  <done>Spacing.kt and Shapes.kt created. xxxl=48dp per DS-04. Bubble shapes are asymmetric per spec. Project compiles. DS-04 and DS-05 tests pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Elevation.kt and Motion.kt</name>

  <read_first>
    - .planning/research/FEATURES.md lines 473-484 (elevation scale table — levels 0-5)
    - .planning/research/ARCHITECTURE.md lines 183-258 (KotoElevation and KotoMotion code patterns)
    - .planning/phases/01-design-system-foundation/01-RESEARCH.md (Motion specs code example)
  </read_first>

  <files>
    android/app/src/main/java/run/koto/ui/theme/Elevation.kt,
    android/app/src/main/java/run/koto/ui/theme/Motion.kt
  </files>

  <action>
    STEP 1 — Create android/app/src/main/java/run/koto/ui/theme/Elevation.kt:

    ```kotlin
    package run.koto.ui.theme

    import androidx.compose.runtime.Immutable
    import androidx.compose.ui.unit.Dp
    import androidx.compose.ui.unit.dp

    /**
     * Elevation tokens for KotoTheme.
     * Access via KotoTheme.elevation.* — use with Surface(tonalElevation = ...).
     *
     * Levels match FEATURES.md elevation scale table (0dp through 16dp for expanded sheets).
     */
    @Immutable
    data class KotoElevation(
        val none    : Dp =  0.dp,   // flat surfaces, backgrounds
        val low     : Dp =  1.dp,   // cards in lists
        val card    : Dp =  2.dp,   // compose bar, navigation bar
        val appBar  : Dp =  4.dp,   // FAB, floating elements
        val fab     : Dp =  6.dp,   // FAB elevation
        val dialog  : Dp =  8.dp,   // context menus, dropdowns
        val pressed : Dp =  8.dp,   // interactive press elevation
    )
    ```

    STEP 2 — Create android/app/src/main/java/run/koto/ui/theme/Motion.kt:

    The ROADMAP Phase 1 success criterion 5 states: "KotoMotionSpec object exists with named
    spring constants used by at least one animated composable." This file is that object.

    ```kotlin
    package run.koto.ui.theme

    import androidx.compose.animation.core.FastOutSlowInEasing
    import androidx.compose.animation.core.LinearOutSlowInEasing
    import androidx.compose.animation.core.Spring
    import androidx.compose.animation.core.SpringSpec
    import androidx.compose.animation.core.TweenSpec
    import androidx.compose.animation.core.spring
    import androidx.compose.animation.core.tween
    import androidx.compose.runtime.Immutable

    /**
     * Motion design tokens for KotoTheme.
     * Access via KotoTheme.motion.* — no composable should use raw spring() or tween() calls
     * with numeric values. All animation specs are named and centralized here.
     *
     * Phase 1 success criterion: KotoMotionSpec pattern in place with named spring constants.
     * FEATURES.md motion table: micro=150-200ms/spring-400, standard=300ms/spring-300, emphasized=400ms/spring-200.
     *
     * Anti-pattern: spring(dampingRatio = 0.7f, stiffness = 300f) inline in composable.
     * Correct:       KotoTheme.motion.springSnappy
     */
    @Immutable
    data class KotoMotion(
        // ── Spring specs (physics-based — use for gesture responses, element entrances) ──
        val springSnappy : SpringSpec<Float> = spring(
            stiffness   = Spring.StiffnessMediumLow,
            dampingRatio = Spring.DampingRatioNoBouncy,
        ),
        val springBouncy : SpringSpec<Float> = spring(
            stiffness   = Spring.StiffnessMedium,
            dampingRatio = Spring.DampingRatioLowBouncy,
        ),
        val springGentle : SpringSpec<Float> = spring(
            stiffness   = Spring.StiffnessLow,
            dampingRatio = Spring.DampingRatioNoBouncy,
        ),
        // ── Tween specs (duration-based — use for explicit UI state transitions) ───────
        val tweenFast   : TweenSpec<Float> = tween(
            durationMillis = 150,
            easing         = FastOutSlowInEasing,
        ),
        val tweenMedium : TweenSpec<Float> = tween(
            durationMillis = 300,
            easing         = FastOutSlowInEasing,
        ),
        val tweenSlow   : TweenSpec<Float> = tween(
            durationMillis = 450,
            easing         = LinearOutSlowInEasing,
        ),
    )

    /**
     * Default KotoMotion instance — provided via LocalKotoMotion in KotoTheme.
     */
    val DefaultKotoMotion = KotoMotion()
    ```

    STEP 3 — Verify compile:
    cd /c/Users/vnaio/Desktop/koto/android && ./gradlew :app:assembleDebug 2>&1 | tail -10
  </action>

  <verify>
    <automated>cd /c/Users/vnaio/Desktop/koto/android && ./gradlew :app:assembleDebug 2>&1 | grep -E "BUILD (SUCCESSFUL|FAILED)"</automated>
  </verify>

  <acceptance_criteria>
    - android/app/src/main/java/run/koto/ui/theme/Elevation.kt exists
    - Elevation.kt contains "@Immutable" and "data class KotoElevation"
    - Elevation.kt contains "val dialog  : Dp =  8.dp"
    - android/app/src/main/java/run/koto/ui/theme/Motion.kt exists
    - Motion.kt contains "@Immutable" and "data class KotoMotion"
    - Motion.kt contains "val springSnappy" and "val springBouncy" and "val springGentle"
    - Motion.kt contains "val tweenFast" and "val tweenMedium" and "val tweenSlow"
    - Motion.kt contains "val DefaultKotoMotion = KotoMotion()"
    - ./gradlew :app:assembleDebug → BUILD SUCCESSFUL
  </acceptance_criteria>

  <done>
    Elevation.kt and Motion.kt created. KotoMotion has 3 spring specs + 3 tween specs. DefaultKotoMotion
    is the named instance. Phase 1 success criterion 5 (KotoMotionSpec pattern) is structurally satisfied.
    Project compiles.
  </done>
</task>

</tasks>

<verification>
1. `grep "val xxxl : Dp = 48.dp" android/app/src/main/java/run/koto/ui/theme/Spacing.kt` → match
2. `grep "val springSnappy" android/app/src/main/java/run/koto/ui/theme/Motion.kt` → match
3. `grep "val DefaultKotoMotion" android/app/src/main/java/run/koto/ui/theme/Motion.kt` → match
4. `grep "val bubbleOut" android/app/src/main/java/run/koto/ui/theme/Shapes.kt` → match
5. `cd android && ./gradlew :app:testDebugUnitTest --tests "*.KotoThemeTest.spacing*"` → all pass
</verification>

<success_criteria>
- Spacing.kt: xxs=2dp, xs=4dp, sm=8dp, md=12dp, lg=16dp, xl=20dp, xxl=24dp, xxxl=48dp
- Spacing.kt: messenger semantic tokens (bubblePaddingH=14dp, bubblePaddingV=10dp, etc.)
- Shapes.kt: sm=8dp, md=12dp, lg=16dp, xl=20dp, bubble=18dp, asymmetric bubbleOut/bubbleIn
- Elevation.kt: 7 levels from none=0dp to dialog=8dp
- Motion.kt: springSnappy/springBouncy/springGentle + tweenFast/tweenMedium/tweenSlow + DefaultKotoMotion
- All @Immutable annotated
- Project compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-04-SUMMARY.md`
</output>
